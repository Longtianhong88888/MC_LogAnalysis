希望这个工具页面的视觉风格更贴近 Apple 设计风格（Human Interface Guidelines）。当前截图的问题在于：圆角不统一、阴影过重、字体层级混乱、间距不够呼吸、且控件显得过于“笨重”。

Apple 风格的核心是：干净、极简、大量留白、纤细字体、微妙的分隔、以及符合直觉的焦点引导。

以下是针对你当前截图的纯视觉与交互细节改善方案（不改变功能逻辑）：

1. 整体框架：去掉“大卡片”思维，改用“轻量化背景”
现状问题：整个页面像一张巨大的带阴影的白色卡片嵌在灰色背景里，显得厚重。

改善方案：

主背景：使用 浅灰白色（#F5F5F7） 或 系统级毛玻璃效果。

内容区：去掉大圆角大阴影的容器，改为平铺在背景上，仅用 极浅的灰线（#E5E5EA） 或 大间距 来区分功能区域。

圆角规范：所有输入框、按钮的圆角统一为 10px（Apple 默认控件圆角），不要出现大小不一的圆角。

2. 顶部标题栏：由“重”变“轻”
现状：“MC Log Analysis Tool” 加粗大标题，下方紧接“日志分析 / 联网报告(PPT)”，显得拥挤。

改善（仿 Apple 风格）：

标题：使用 SF Pro Display 字体（或系统默认字体），Weight: Semibold，字号 34px，颜色 #1D1D1F。

副标题/面包屑：将“日志分析 / 联网报告(PPT)”改为 Page Title 下方的轻量级描述，使用 Regular 字体，字号 13px，颜色 #86868B。

排版：标题与下方操作区之间留出 至少 32px 的垂直间距。

3. 核心操作区：采用“标签式分组”与“内联布局”
现状：输入框纵向排列，每个占一整行，显得空旷且低效。

改善（仿 Apple 系统偏好设置布局）：

第一行（核心必填）：采用 Flex 左对齐布局。

站点 标签 + 下拉框（宽度 150px）

机台号 标签 + 输入框（宽度 220px）

右侧紧贴「查询数据」按钮（蓝色主按钮，Weight: Medium，圆角 10px）

第二行（时间）：采用 内联日期选择器。

开始时间 + 日期选择框（宽度 200px） → 至 → 结束时间 + 日期选择框（宽度 200px）

右侧放置一个 🔄 重置到今天 的轻量级文字按钮（无边框，仅蓝色文字）。

第三行（高级参数）：采用 Apple 风格的“显示更多”箭头。

点击 ▶ 高级参数 后，平滑展开下方参数行（接口、PlantID、Head、计划时间、停机Reason 平铺在一行内，用 · 点分隔）。
4. 控件样式：去掉一切“多余装饰”
下拉框 / 输入框：

背景色：#FFFFFF，边框：1px solid #D2D2D7。

聚焦状态：边框变为 #007AFF（Apple 蓝），并伴有 极淡的蓝色外发光（0 0 0 3px rgba(0,122,255,0.2)），而非加粗边框。

文字占位符（Placeholder）：使用 #AEAEB2。
按钮：

主按钮「查询数据」：背景 #007AFF，白色文字，无阴影。

次按钮「输出报告」：去掉背景色，仅保留 1px 灰色边框 + 深灰色文字（#1D1D1F）。

禁用状态：输出报告在没有数据时，文字颜色变为 #AEAEB2，边框变为 #E5E5EA，且无点击反馈。

5. 状态与日志区：采用“三段式轻量信息条”
现状：“当前状态/数据行数/最后查询”三个信息横条与“日志输出”混在一起，不够精致。

改善（仿 Apple Music 或 Xcode 的状态栏）：

顶部状态条：使用三个 轻量胶囊标签 并排显示：

● 状态：已就绪（绿色圆点表示正常）

📋 数据行数：0

🕒 最后查询：--

分割线：下方用一条 1px 的 #E5E5EA 细线分隔。

日志输出框：去掉输入框那样的粗边框，改为 浅灰色背景（#F5F5F7） 的圆角区块，内部文字使用等宽字体（Menlo 或 Courier），字号 11px，颜色 #3A3A3C，带内边距。

数据预览表：表格表头背景色用极浅灰（#F9F9FB），表头文字加粗但字号小（11px），表格线用 #E5E5EA，且默认只显示 5行（而非10行），避免信息过载。

6. 输出路径：采用“Finder 风格”选择器
现状：“输出路径：未选择” + 一个单独的“选择保存路径”按钮，略显割裂。

改善：

设计成类似 macOS 保存对话框的样式：📁 存储为： [ 报告_20260813.pptx ] + 右侧一个 ▼ 下拉箭头用于选路径。

或者将“选择保存路径”改为带下划线的 蓝色文字按钮，点击后弹出原生文件夹选择框。

7. 配色与字体规范（对标 Apple 官网）
元素	Apple 风格值
主标题色	#1D1D1F
副标题/标签色	#86868B
输入框占位符	#AEAEB2
主操作蓝	#007AFF
分割线/边框	#E5E5EA / #D2D2D7
警告/危险（停机）	#FF3B30
成功（运行）	#34C759
字体	-apple-system, BlinkMacSystemFont, "SF Pro Display"
行高	1.5 倍，保证呼吸感
8. 底部版权信息
现状：黑色大字“就绪” + “Copyright © 2026 ABU NPD EOL”，干扰视线。

改善：

将“就绪”状态合并到状态栏中（见第5点）。

版权信息改为 极小字（10px），颜色 #AEAEB2，右对齐，与主内容区有明显间距（margin-top: 48px）。

总结：Apple 风格的核心改动清单
去阴影、去大圆角、去重边框。

将纵向堆叠改为横向内联，减少滚动。

将“输出报告”按钮默认置灰，必须查询到数据后才变亮。

高级参数默认折叠，保持主界面极简。

增加聚焦动画（输入框变蓝发光）和 平滑展开动画（高级参数）。